- **Maya is the only person on a four-person team who cares about QA**, and she ships an AI-built internal tool most Fridays. She fixes eight bugs, ships, and next Friday three of the new bugs are the same kind she already fixed, wearing different symptoms; she knows it, and she pays full price to rediscover them anyway.
- **Ratchet is Maya's instinct, automated.** It watches bugs arrive, fixes them, and keeps every fix that was _proven_ to work, so the second time that kind of bug shows up it costs one cheap step instead of four expensive ones: $0.43 down to $0.03. The word "proven" is the whole product, because a fix only enters memory after Replay re-tests and confirms it, so what accumulates is verified outcomes rather than things a model merely claimed.
- **You are showing three screens and nothing else: a terminal, a dashboard, and Replay's own UI.** The terminal runs the loop live. The dashboard is one page with a two-line chart, a cold-versus-warm split pane, and a memory panel listing every pattern and what it has saved. Replay's UI is where a judge watches your bug flip to "fixed" in _their_ system, which is the moment your claim stops being your word for it.
- **The three minutes go: Maya, then one bug twice, then the counterfactual, then the closer.** Twenty-five seconds on Maya with no screen at all. Then the same bug class cold at four calls and $0.418, and warm at one call and $0.030, side by side, with "14× cheaper, same root cause, different bug" underneath. Then both arms on one chart. Then the closer, which is the second app.
- **The closer is the part nobody else in that room can show.** You point Ratchet at `jotting-fixture`, a notes app from a different company that it has never seen, carrying the same latent modal bug in two disguises: a share invite that goes to the wrong note, and a tag that resurrects after deletion. Its very first run is already cheap, because a pattern verified on Tasker transferred. That is Maya's fix making a stranger's first Friday cheap.
- **The proof is a second agent running beside Ratchet on identical bugs with memory switched off, which is exactly Maya's Friday today.** Ratchet falls from $0.4258 to $0.0293 per fix; the control climbs to $0.8643, because it keeps re-failing on classes it has already seen. Looping without memory does not plateau, it degrades, matching a published finding of vulnerabilities rising 37.6% over five unmemoried iterations. Say the honest bits out loud too: Replay independently found a bug you had seeded, your system scored it 0.49 against a known pattern, your bar is 0.60, so it went cold and paid full price. You would rather miss a warm hit than fake one, and that is why the rest of the chart can be trusted.
- **Four sponsors are live, each owning exactly one part of the loop, and one was investigated and cut.** Replay QA crawls the deployed app, returns real root-caused bugs, re-verifies the fix, and takes our confirmation back into their system. Actian VectorAI DB runs in Docker as primary memory, holding each verified fix as a vector so bugs match by meaning rather than keyword. Senso Context OS publishes each verified pattern to a public citable page. Pioneer routes every call, and their response tells you what it saved by picking a cheaper model than the baseline, which stacks with your own saving: Pioneer makes each call cheaper, you delete three calls in four. Guild you investigated and cut, because their LLM proxy only exists inside a Guild-hosted runtime and their coded agents are TypeScript-only; naming that tradeoff scores better than a token integration. None of this composition is individually new, since EvoRepair reports −41.58% and Meta's Experience Graphs reports 52% lower token cost from verified-experience reuse, but every published version is single-organization, so the bet is that the economics only get interesting when the corpus crosses company lines.