---
description: Load the current brief from the Architect and start executing
---
1. Read `MISSION.md` (skim if already read this session — but re-read the clock and judging criteria).
2. Read `.cowork/BRIEF.md` in full. It is overwritten every round; never rely on memory of a previous brief.
3. Read the last two entries of `.cowork/LOG.md` so you know what state the repo is actually in.
4. Restate the brief back in 3 bullets: goal, done-criteria, time-box. Flag immediately if it conflicts with what's on disk.
5. Execute. Commit as you go. Run `/handoff` when done or blocked.
