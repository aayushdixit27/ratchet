# ALL-012 — supersedes ALL-011's lane assignments
**Issued 13:35. ~1h25 to freeze. Two hard decisions below, both pre-committed.**

## DECISION 1 — hero metric: build the real 4-call cold path. Time-boxed.
Live cold currently makes **1** call, not 4. That means our claim ("cold = full reasoning from scratch") is not true of the running system; the fixture was describing a cold path we intended and never built.

**Lane A takes option 1:** cold issues four genuine router calls — localize, hypothesize, synthesize, self-review. Warm stays 1. Calls-per-fix then falls **4 → 1 monotonically on live data with real model names**, and it is vendor-independent forever. It also produces better fixes, which is the honest reason to want it.

**HARD FALLBACK, pre-committed now: if this is not green by 14:15, revert to option 2.** Hero chart reverts to the fixture golden; the live run supplies real model names for the cold pane and badges only; we disclose it verbally with the seeded-corpus framing. **Do not make this decision at 14:40.**

Option 3 (recording `llm_calls=4` while making 1 real call) is rejected — one `inference_id` per record exposes it, and it is the exact fabrication this brief exists to prevent.

## DECISION 2 — the embedding change must be justified before freeze. Highest priority.
The counter bug scored **0.49** at ~12:30. After commit `cefc725` ("embed axes for app2's bug classes") it now warm-hits at **0.76**. That is a 0.27 jump on the same bug, and it costs us our single best honesty beat: *"we scored it 0.49, our bar is 0.60, so it paid full price."*

It may be entirely legitimate — the original embedding was crude and this could be a genuine fix to a bad miss. But from outside, legitimate and tuned-to-fit look identical. **Lane B answers three questions in the log before freeze:**
1. Does Tasker's original separation still hold? Same-class ~0.78–0.80, nearest non-match ~0.42. If those moved, we broke what we already proved.
2. What vocabulary was added, and why does it describe the **class** rather than the **instance**? An axis named after a concept is fine. An axis that only matches one bug is not.
3. Was the proposed cross-app threshold **0.75** chosen on a principle, or after seeing the 0.76 score? It currently sits one hundredth under a hit, and a technical judge who notices will ask.

**If any answer is unclean, revert the embedding.** A live money shot is worth a lot. It is not worth more than the threshold being honest, because the threshold is what makes every other number believable.

## Lane assignments

### Lane A
1. **4-call cold path** (decision 1), hard stop 14:15.
2. Re-run, verify the five checks from A-003 — with **calls-per-fix monotonic** replacing cost-per-fix in check 1.
3. **Transfer run: `--target app2 --corpus-from tasker`.** Lane C is blocked on this JSONL for the ACME → GLOBEX banner.
4. Confirmed by Lane B: keep **QA fixture** in the candidate run. Live QA has no warm path and fails the warm-share check. The live scan is a separate demo beat.

### Lane B
1. **Decision 2 — answer the three embedding questions. This outranks everything else in your lane.**
2. Cheap tier is still `pioneer/fixture-fast` in live records — `RATCHET_ROUTER_MODE=live` appears wired to strong only. Fix so no `fixture` string survives.
3. Telemetry capture: **done**, nothing further.

### Lane C
1. Hero chart becomes **calls per verified fix**, two arms, same labels. Build it to read whichever golden is in place at 14:15 — do not hardcode a source.
2. Cost moves to a stat tile as a range, with Pioneer's `rate_diff_per_mtok` savings beside it.
3. `$0.000` rows render as **"no baseline yet"**. Never a computed fallback.
4. **ACME → GLOBEX banner** once Lane A's app2 JSONL lands. Loudest thing on screen.

## Architect owns
Re-pulling every number quoted in `DEMO.md` against the final render after 14:15. Nobody else touches that file.
