# BRIEF B-010 — Pioneer is live, and it returns its own savings telemetry. Capture it.
**Issued 12:55. Four sponsors live. ~2h to freeze — this is a capture-and-record task, not an architecture task.**

## What their response actually contains
```json
"model": "deepseek-ai/DeepSeek-V3",
"usage": {"prompt_tokens": 4, "completion_tokens": 12, "total_tokens": 16},
"x_pioneer": {
  "inference_id": "704e61b6-...",
  "routed_model": "deepseek-ai/DeepSeek-V3",
  "savings": {
    "baseline_model": "gpt-5.5",
    "routed_model": "deepseek-ai/DeepSeek-V3",
    "rate_diff_per_mtok": {"input": 4.73, "output": 28.88, "cache_read": 0.365, "cache_write": 4.73}
  }
}
```
**Pioneer computes and returns its own per-call saving**, naming the baseline model it avoided. That is a sponsor's own API validating a cost claim for us. We are not estimating anything.

## Do this
1. **Capture every `x_pioneer` field into each record:** `routed_model`, `baseline_model`, `rate_diff_per_mtok`, `inference_id`, plus real `usage` token counts. `inference_id` is our audit trail if a judge asks whether the calls were real.
2. **`model` in each record must be the `routed_model` Pioneer actually used**, not what we asked for. That is also how we prove cheap and strong resolved to genuinely different models — keep the same-model refusal guard on.
3. **Compute two separate savings figures and keep them separate:**
   - `router_saved_usd` — Pioneer's number: what their routing saved versus the baseline model, per call.
   - `calls_eliminated_saved_usd` — ours: the cold-path cost we did not pay because memory produced a warm hit.
4. **Fill `PIONEER_STRONG_MODEL`** with a real frontier ID from `/v1/models`. Their baseline reference is `gpt-5.5`, so pin something in that class for the cold path and leave `pioneer/auto` as cheap.
5. Re-run and **confirm the curve still declines and the control arm still degrades with real prices.** Report both fixture and live numbers. **Do not tune fixtures to match.** If real pricing changes the shape, tell the Architect before doing anything about it.

## Why this matters to the pitch
`POSITIONING.md` says *"Pioneer makes every call cheaper. We delete the call."* We can now **show both, stacked, with their telemetry proving the first half**:
> "Pioneer's router saved us $X on this call by picking DeepSeek-V3 over gpt-5.5. Then our memory removed three of every four calls entirely. Those savings multiply, they don't compete."

That turns Pioneer from a dependency into a co-star, and Kelton is judging.

## ⚠️ Budget guard — do this before any long run
A card is now on file and **auto-recharge is set to charge $50 when the balance hits $10.** A runaway loop against real inference can trip it.
- Keep `PIONEER_MAX_TOKENS` capped.
- Cap total live calls per run and log the running spend.
- Check `Spent today` in the console after the first full live run.
- **Tell Yoshi to switch auto-recharge OFF in the console.** $40 of credit is far more than this needs.
