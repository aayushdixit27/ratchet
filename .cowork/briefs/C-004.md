# BRIEF C-004 — the dashboard is genuinely good. Three fixes, in priority order.
**Issued 13:05. I rendered it headless at 1920×1080 and read it as a judge would. Split pane, memory panel, two-arm chart, warm/cold share: all present and all working. This is strong work.**

## 🔴 1. `pioneer/fixture-strong` is on screen — highest priority, and it is NOT yours to fix alone
The cold pane shows four rows reading `LLM call 1 / 4 · pioneer/fixture-strong`. On a projector, a judge reads the word **fixture** while we are saying four sponsors are live. That is the exact demo-versus-build gap the judging guidance says they hunt for.

**Fix at the source, not in the renderer:** Pioneer is live as of 12:50. `runs/golden/` needs regenerating with `RATCHET_ROUTER_MODE=live` so the model label is the real `routed_model` Pioneer returns (e.g. `deepseek-ai/DeepSeek-V3`). **That is Lane A + Lane B's job** — flag it in your handoff, do not paper over it in the dashboard.

Until they regenerate, render whatever is in the JSONL. **Never relabel a fixture as live.**

## 🟡 2. Two rows both say `modal-state-not-reset`
Rows 2 and 4 of the memory panel share a class name with different savings ($1.16 and $0.776). To a judge that reads as a deduplication bug in the thing whose entire job is recognising that two bugs are the same.

Either merge them into one row with combined `uses` and `saved_usd`, or disambiguate visibly — append a short `pattern_id` so it's obviously two distinct patterns within one class, not the same pattern listed twice. **Merging is the stronger story**: one pattern, six uses, $1.94 saved.

## 🟢 3. Warm/cold share labels are ambiguous
Iteration 0 shows **20%** printed above a mostly-*red* bar. The legend says green is warm, so 20% refers to the small green sliver, but the number sits on the red. Put the percentage on the green segment, or label it `20% warm`. Five-minute fix, removes a "wait, which is which" moment mid-demo.

## What's already right — don't touch it
- Two-arm chart with the control line rising to **$0.864** and the arms labelled exactly as scripted. That's the strongest single fact we have and it's unmissable.
- `14× cheaper — same root cause (modal-state-not-reset), different bug` under the split pane. That one line does the whole argument.
- Stat tiles: `$5.07 saved`, `1.0 LLM calls per fix (was 3.3)`, `6 patterns reused 22 times`.
- Contrast, size and legibility are right for a dim room.

## Then, and only then
App #2 per `AMENDMENT-03`. If it's a choice between app #2 and fixing item 1, **fix item 1** — a real routed model on screen is worth more than a second app.
