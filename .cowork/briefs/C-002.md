# BRIEF C-002 — build the two things the demo actually shows
**Issued 12:40. 2h20m to the 15:00 freeze. The join is green, so AMENDMENT-02's gate has lifted.**

The dashboard currently renders the hero chart and knows about `control`. **It does not have the split pane or the memory panel** — and those are 40 of the demo's 180 seconds. Read `DEMO.md` and `USER-STORY.md` first; every screen in that script has to exist.

## Order is deliberate. Do 1 completely before starting 2.

### 1. Split pane + memory panel — ~45 min. This serves the *whole* demo.
`DEMO.md` 1:15–1:55 is the single longest beat and there is currently nothing to show during it.

**Split pane**, two columns, same bug class:
- **Left, cold:** the reasoning transcript scrolling, call count, cost, elapsed. Let it feel slow — the slowness is the setup for the right-hand side.
- **Right, warm:** one line, big — `MEMORY HIT · pattern #3 · modal-state-not-reset · similarity 0.91 · learned at iteration 1 · used 4×` — then the cost. That's it. The contrast between a wall of reasoning and a single line *is* the point.

Fields you need in the JSONL: `similarity`, `pattern_id`, `uses`, `path`. If Lane A isn't emitting them yet, say so in a handoff immediately — **don't work around it and don't invent them.**

**Memory panel:** every pattern with `bug_class`, `born_at_iteration`, `uses`, `saved_usd`, and a `discovered_by` source badge. **Sort by `saved_usd` descending.** The top row should let someone read "this one pattern has saved $3.71" from fifteen feet.

**Two-line hero chart:** label the arms **"Replay + coding agent (today)"** and **"Ratchet"**. The control line goes *up* — to $0.8643 — and the chart must make that unmistakable, because it's the strongest single fact we have. Don't clip the y-axis in a way that hides it.

### 2. App #2 — cross-org transfer — ~45 min, start only when 1 is done
Per `AMENDMENT-03`. A notes app, different domain, different DOM, different copy, carrying **the same latent root cause** (lazy `x = x || seed()` scratch state that `closeModal()` never nulls), with symptoms that look completely unrelated to Tasker's. Six bugs, two sharing the modal class, rest novel. `app2/bugs.json` in the schema Lane B already consumes. Second GitHub Pages URL — Pages survives machine sleep, the tunnel doesn't.

**If you run out of time, this is what gets cut, not item 1.** Say so early rather than delivering both half-done.

## Constraints
- Dim room, projector, fifteen feet, tired judges at 19:00. Contrast and size over elegance. No animation that delays comprehension.
- You own `app/**`, `app2/**`, `dashboard/**`. Nothing under `src/` or `runs/`.
- Commit every 15 minutes.
- **Everything must render from `runs/golden/` with zero network.** That's the wifi-dies path and it is not optional.

Run `/handoff C` when item 1 is done — don't wait for both.
