# BRIEF C-005 — presentation mode. One surface, arrow keys, no jumping.
**Issued 15:10. Yoshi rehearsed with the Replay CEO: concepts land, delivery is scattered. Root cause is three surfaces plus scrolling.**

The Architect has built a **working reference deck** and sent it to Yoshi as `deck.html`. Ask him for it. **Do not redesign it — wire it to real data.**

## The problem it fixes
The demo currently requires: Replay tab → terminal → dashboard → scroll to split pane → scroll to chart → scroll to transfer. Six context switches in 180 seconds, driven by hand, under pressure, in front of judges. That is why it feels scattered, and no amount of visual polish fixes it.

## The fix
**One browser tab. Five full-screen beats. Right-arrow to advance. Nothing else.**

| Beat | Content | Data source |
|---|---|---|
| 1 | Replay's two duplicate counter bugs, side by side, + "consolidate before fixing" | static text from Replay's dashboard |
| 2 | Cold 4 calls / $1.65 vs warm MEMORY HIT / $0.03 | `runs/golden/ratchet.jsonl` |
| 3 | Calls-per-verified-fix, two arms, control rising 5 → 8 | `ratchet.jsonl` + `control.jsonl` |
| 4 | ACME → GLOBEX, 2/4 warm on first run, similarity 0.837 | `runs/golden/transfer.jsonl` |
| 5 | Real terminal output, both arms, including the losing one | paste real `--demo` output |

Built in already: keyboard nav (→ ← space PageUp/Dn), beat counter, progress rail, and a **live timer that turns amber at 2:30 and red at 3:00**. `r` resets it.

## Your job
1. Take `deck.html` as-is. Replace the hardcoded numbers in beats 2, 3, 4 with values read from `runs/golden/*.jsonl` at load, exactly as the current dashboard does. **Same rule as always: render what the data says, never hardcode.**
2. Beat 3's SVG is hand-plotted at fixed coordinates — make it compute from the data so it can't go stale.
3. Beat 5: paste the **real** terminal output from a genuine `--demo` run. Keep the `backends={...}` line visible. Honesty is the point of that beat.
4. **Keep the existing `dashboard/index.html` untouched and working.** The deck is the *presentation*; the dashboard stays as the detailed artifact for judges who want to dig after the three minutes. Two different jobs.

## Design constraints — these came from the dataviz method, don't override them
- Palette is validated: **Ratchet `#3987e5`, control `#d95926`** on surface `#1a1a19`. Categorical slots 1 and 2 — colourblind-safe adjacent pair. Do not swap in red/green.
- `#0ca30c` is reserved for the warm-hit state only, never as a series colour.
- Two series → legend present **and** direct labels on the end points. Identity never by colour alone.
- One idea per screen. If a screen needs a second sentence to explain it, it's two screens.
- Text stays in ink tokens (`#ffffff`, `#c3c2b7`, `#898781`), never in a series colour.
- Read from fifteen feet, dim room, projector. Nothing under 14px.

## Time
Wire it to data first. Polish second. If you only get beats 2, 3 and 4 reading live data, that is a complete win — 1 and 5 are static content by design.
