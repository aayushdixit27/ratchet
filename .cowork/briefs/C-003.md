# BRIEF C-003 — YOU ARE UNBLOCKED. The gate lifted 40 minutes ago.
**Issued 12:52. Read this before anything else.**

Your last handoff says *"runs/ is still empty, so the join hasn't moved since 11:33."* **That is stale.** State as of right now:

- `runs/ratchet.jsonl` — **23 rows**. `runs/control.jsonl` — **29 rows**. Both archived to `runs/golden/`.
- The curve is real: **$0.4258 → $0.0293**, control arm climbs to **$0.8643**.
- Four sponsors live: Replay, Actian, Senso, Pioneer.
- **Every field you asked for is already emitted.** Lane A delivered all of them:

```json
{"path":"warm","similarity":0.8565,"pattern_id":"b7a3887a7811","uses":2,
 "cost_usd":0.029707,"arm":"ratchet","saved_usd":...,"origin_app":...,"origin_org":...}
```

Nothing is blocking you. Go.

## Priority — unchanged from C-002, now urgent
1. **Split pane + memory panel + two-line chart.** ~45 min. This is 40 of the demo's 180 seconds and it currently does not exist.
2. **App #2.** Only after 1 is complete. **If time runs short this is what gets cut** — say so early rather than delivering both half-done.

## ⚠️ Render from the data. Never hardcode.
`DEMO.md` previously quoted *"similarity 0.91 · learned at iteration 1 · used 4×"*. **Those were placeholder numbers invented before the data existed.** The real values are `similarity: 0.8565`, `pattern_id: b7a3887a7811`, `uses: 2`. I have corrected the script.

If a number on screen doesn't match `runs/golden/ratchet.jsonl`, that is precisely the demo-versus-build gap judges are briefed to hunt for. Read every value from the JSONL at render time.

## Also
- The control line goes **up**, to $0.8643. Do not let y-axis scaling hide that — it's the strongest single fact we have.
- Everything must render from `runs/golden/` with **zero network**. That's the wifi-dies path.
- Label the arms **"Replay + coding agent (today)"** and **"Ratchet"**.
- Freeze is **15:00**. You have ~2 hours.
