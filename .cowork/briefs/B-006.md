# BRIEF B-006 — GO LIVE. Keys are in.
**Issued 12:20. `REPLAY_API_KEY`, `PIONEER_API_KEY` and `SENSO_API_KEY` are all present in `.env`.**
**Guild is cancelled — see B-005. Do not touch it.**

## ⛔ STEP 0 — before you make a single live call
```
mkdir -p runs/golden && cp runs/ratchet.jsonl runs/control.jsonl runs/golden/
git add -A && git commit -m "chore: archive golden fixture run (demo insurance)"
```
We currently have a **working demo**: a real declining curve, a control arm that degrades, zero network, exit 0. That is the thing we present if everything else burns down. Live integrations must never be able to destroy it. `--demo` replays from `runs/golden/`.

**Never overwrite `runs/golden/` again today** unless a live run is verified better *and* the Architect says so.

## Order — one at a time, verify each before starting the next

### 1. PIONEER first (unblocks everything else's cost numbers)
```
curl -H "X-API-Key: $PIONEER_API_KEY" https://api.pioneer.ai/v1/models
```
Pick a genuinely small model for `cheap` and a frontier one for `strong`. Put the real namespaced IDs in `.env`. The same-model refusal guard you built stays on — **if cheap and strong resolve to the same model the chart is a lie**. Log both into every record's `model` field.
Then: `RATCHET_ROUTER_MODE=live python3 -m ratchet.run --iterations 5`
Expect real cost numbers to differ from fixture. **That's fine and good — do not tune fixtures to match.** Report both.

### 2. REPLAY second (this is the positioning)
Target is already set: `RATCHET_TARGET_URL=https://aayushdixit27.github.io/tasker-fixture/`
```
POST /api/v1/projects            -> create + start exploration
GET  /api/v1/projects/{id}/status  -> poll (this takes MINUTES, not seconds)
GET  /api/v1/projects/{id}/bugs
GET  /api/v1/bugs/{bug_id}
PATCH /api/v1/bugs/{bug_id}      -> mark_fixed write-back
```
**Save the project id into `REPLAY_PROJECT_ID`** so later runs reuse it instead of re-exploring — re-exploration is the slowest thing in our stack and we cannot afford it during the demo.
**Cache the raw bug JSON to disk the moment it arrives.** That cache is what `--demo` replays. The exploration must succeed *once*; after that we never depend on Replay's latency again.
Then verify `mark_fixed` actually flips status in Replay's UI — that's a demo beat at 2:30.

### 3. SENSO third
Needs `SENSO_GEO_QUESTION_ID` — get one from the dashboard. Absent it, degrade to local `cited.md`, don't block.
Use `senso engine draft` while testing. Only `senso engine publish` on the final demo run.

### 4. ACTIAN — Yoshi is on it (Docker + Community Edition, or a hosted endpoint from their table)
The moment `ACTIAN_ENDPOINT` responds: `RATCHET_MEMORY_MODE=live`. This is the thesis; it's worth waiting for.

## Non-negotiables
- **Per-adapter mode flags only.** Never set `RATCHET_MODE=live` globally today.
- Any live failure → degrade to fixture, `degraded: true`, never re-raise, never block the loop.
- Replay's polling needs a hard timeout that degrades. **The loop must not sit waiting on a sponsor API.**
- After each adapter goes live, re-run and confirm **the curve still declines and the control arm still degrades**. If a live integration flattens the curve, tell the Architect immediately — do not quietly adjust anything to compensate.
- Commit after each adapter lands.

Report which of the four are genuinely live, with one real response logged per sponsor as evidence. That count is a scored judging criterion and I need the honest number, not the optimistic one.
