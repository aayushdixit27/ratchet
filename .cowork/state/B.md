# LANE B — working state as of 13:46

## 1. Identity
Own: `src/adapters/**`, `.env.example`, `.env` (values), `src/adapters/seeds/**`.
Never touch: `src/ratchet/**`, `app/**`, `app2/**`, `dashboard/**`, `runs/golden*/**` (read-only), `.cowork/**` except LOG via /handoff, `MISSION.md`, `PRODUCT.md`, `DEMO.md`.
Model: Fable 5 (fine for adapter work; switch to Opus for live-API surprises or demo-freeze debugging).

## 2. Ground truth right now
- ALL FOUR sponsors are LIVE with evidence: Replay (project `proj-ratchet-mrzbmsos`), Senso (CLI draft mode, org ScoutAI), Actian (Docker `vectorai` container, gRPC 6574), Pioneer (Bearer auth works, X-API-Key also works; billing was a workspace-key issue, resolved).
- `.env` is auto-loaded by `src/adapters/__init__.py` — no `source .env` needed anywhere, explicit env still wins. Guild is CANCELLED (B-005); tracer stays fixture.
- Pioneer tiers: cheap=`pioneer/auto` (routes ~$0.000005/call), strong=`gpt-5.5` pinned. Same-model refusal guard + $5/`400`-call budget caps live in the adapter.
- Retrieval thresholds: same-app **0.55**, cross-app (`--corpus-from`) **0.75** (Lane A decision pending on wiring the second). Matrix: tasker mates 0.764–0.773 vs 0.431; transfer mates 0.842/0.846; one known adjacency J-05↔dead-control 0.566 (why 0.75 exists).
- The live counter bug (`bug-mrzceiuz-wr97`) warm-hit at 0.7608 is **corpus-dependent** — must be re-scored against the final golden corpus before the demo scripts that beat.
- Live Replay bugs cached (canonical): `src/adapters/seeds/replay_bugs_live.json` (4 open, 1 judge-rejected as of 12:45). `REPLAY_PROJECT_ID` pinned in .env — never re-explore during demo.
- `runs/golden/` = fixture insurance, `runs/golden-fixture/` may exist too (A-003). NEVER regenerate golden in place.
- Lane A landed 4-call cold path (4a9068d) AFTER my last verification — the curve numbers I logged (live $0.0163→$0.0007) predate it; llm_calls semantics changed.
- cited.md is regenerated by loop runs; my tests once deleted it — always `git checkout -- cited.md` if a test eats it.
- mark_fixed demo bug: `bug-mrzbq4us-w9k6` (toast), status=open, one PATCH from flipping in Replay's UI.

## 3. Commands that work
- Full loop (env auto-loads): `python3 -m src.ratchet.run --iterations 5`
- One adapter live-only: `RATCHET_ROUTER_MODE=live python3 -c "from src.adapters import get_router; ..."` (per-adapter `*_MODE` beats `RATCHET_MODE`)
- Replay status: `curl -H "Authorization: Bearer $REPLAY_API_KEY" https://qa.replay.io/api/v1/projects/proj-ratchet-mrzbmsos/status`
- Replay bug flip: `curl -X PATCH -H "Authorization: Bearer $REPLAY_API_KEY" -H "Content-Type: application/json" -d '{"status":"fixed"}' https://qa.replay.io/api/v1/bugs/<id>`
- Pioneer models+prices: `curl -H "Authorization: Bearer $PIONEER_API_KEY" https://api.pioneer.ai/v1/models`
- Senso (needs PATH): `export PATH="$PATH:$HOME/.npm-global/bin"; senso whoami --output json`
- Actian container: `docker start vectorai` (data persists in `local_data/`)
- Isolated adapter state for tests: prefix `RATCHET_STATE_DIR=/tmp/whatever` (NEVER test against `.ratchet/` — Lane A's runs share it)

## 4. REJECTED — do not retry
- `X-API-Key` for Pioneer inference: BOTH header styles work for identification; the 403 was workspace entitlement. Adapter self-heals (Bearer→X-API-Key retry). Don't relitigate.
- `adaptive: true` param for Pioneer: unverified invention, removed. `pioneer/auto` IS the router.
- camelCase splitting in `_tokens()`: floods location words (modal/note), blurs class boundaries. Documented in embed.py.
- Concept-axis magnitude cap: compresses all margins into 0.55–0.60 band. Documented in embed.py.
- Instance vocabulary on axes (`asymmetric`): deleted; every axis word must describe a CLASS.
- Markdown parsing of app/BUGS.md: silently produced garbage; use `app/bugs.json` (JSON is canonical).
- Guild REST tracer: no public API surface exists (B-003/B-005). `live_guild.py` exists but is dead code; leave it.
- Permanent degrade on first router error: one transient 429 fixture-priced 3 iterations. Now 3-consecutive rule. Don't "simplify" it back.
- Call-count budget cap as primary: tripped mid-run, stitched fixture+live prices into one lying curve. Dollars-first.
- `pip install actian-vectorai`: wrong name. It's `actian-vectorai-client` + `pip install -U 'protobuf>=6.33.5'`.
- Actian search limit=1: HNSW recall hole, returns [] for far queries. Adapter over-fetches 10; don't remove.
- One shared Senso geo_question_id: every pattern overwrites one document. Per-class prompts, cached in state.

## 5. In flight
- Nothing half-done in my tree. Last commits: 124f11a (.env auto-load), 96385b9 (asymmetric removal).
- Waiting on Lane A: per-mode threshold wiring (0.75 cross-app), JSONL passthrough of `router_saved_usd`/`inference_id`/`routed_model` (keys exist in my usage dict), re-score of counter bug vs final golden.
- Next concrete action if idle: poll Replay `bugs` count once (~5 test runs were still exploring); refresh `seeds/replay_bugs_live.json` if changed; re-run the 18-cell matrix if the corpus regenerated.
- Freeze is 15:00. After freeze: demo-path support only.

## 6. Gotchas
- Replay `analysis` is a dict `{'chain':[{text}]}` — `_analysis_text()` flattens; `reproduction_steps` can be null; project id is `id` (proj-…) NOT `exploration_id` (expl-…).
- Senso CLI prints a banner line before JSON — parse from first `{`. Draft mode until the demo run (`SENSO_PUBLISH_MODE=publish`).
- Actian sync client needs explicit `.connect()`; ENGINE_NOT_INITIALIZED right after `docker start` is normal (adapter backs off ~15s).
- Embed changed ⇒ any pre-cefc725 vectors in Actian are stale; loop runs reset+rebuild automatically, ad-hoc searches against an old collection lie.
- `.ratchet/patches*.json` per app; `FixtureQA.reset()` clears only its own app's ledger.
- Fixture `strong` reports `calls: 4`; live semantics now match via Lane A's 4-call cold — but MY fixture router still fakes 4-in-1. Cost comparable, calls now comparable too (post-4a9068d).
- The Architect's briefs have been wrong twice (Pioneer auth, Replay field names) — verify against live responses before implementing any "spec" from a brief.
- Git lock files from the Architect's mount: `rm -f .git/index.lock` is safe.

## 7. Pointers, not content
1. `.cowork/BRIEF.md` / newest `B-0xx.md` — current task (always re-read).
2. `.cowork/LOG.md` — my entries at 11:34, 12:01, 12:28, 12:38, 12:48, 13:04, 13:14, 13:24 tell the whole lane story.
3. `src/adapters/base.py` — the contract; `embed.py` header comments — why hash embeddings.
4. `.cowork/state/GLOBAL-REJECTED.md` — cross-lane rejected list (new, read it).
5. `PRODUCT.md` §5/§6, `MISSION.md` — binding scope + judging.
6. B-briefs 006–010 + ALL-012 — the go-live and integrity decisions in order.
